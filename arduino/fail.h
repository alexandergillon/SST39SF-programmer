#include <Arduino.h>

/**
 * @brief Goes into an infinite loop, printing an error message to serial at regular intervals.
 * 
 * @param errorMessage the message to print
 */
void fail(String errorMessage);